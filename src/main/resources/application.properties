# Supabase PostgreSQL Configuration
# Using Connection Pooler (port 6543) - Works better behind firewalls
spring.datasource.url=${SUPABASE_DB_URL:jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres}
spring.datasource.username=${SUPABASE_DB_USERNAME:postgres.rhebewpkkimrlddyceax}
spring.datasource.password=${SUPABASE_DB_PASSWORD:elm12HSk3YRBOtWf}
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool settings
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=${SERVER_PORT:8081}
